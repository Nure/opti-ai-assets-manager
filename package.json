{
  "name": "ai-dam-migration-copilot",
  "version": "1.0.0",
  "private": true,
  "description": "AI-assisted CMS12 and WordPress to Optimizely CMP DAM migration copilot",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix server && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
